export enum EVENTS {
  shoot = "shoot",
  destroy = "destroy",
  died = "died",
  complete = "complete",
  restart = "restart",
  pause = "pause",
}

export enum WAYS_TO_DIE {
  fell = "fell",
  backfire = "backfire",
  enemy = "enemy",
  spikes = "spikes",
  crashed = "crashed",
  burned = "burned",
}

export enum ANIMATIONS {
  playerJump = "player-jump",
  playerRun = "player-run",
  playerDoubleJump = "player-djump",
  playerWallJump = "player-wjump",
  playerIdle = "player-idle",
  playerFall = "player-fall",
  enemyIdle = "enemy-idle",
  enemyRun = "enemy-run",
  collectableIdle = "collectable-idle",
}
